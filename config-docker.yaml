


cache: kafka

kafka:
    # ip: 'broker'
    # port: 29092
    ip: '203.245.29.236' 
    port: 9092
    schema_registry_ip: '203.245.29.236'
    schema_registry_port: 8081
    topic_key: 'loopyquant01.02-'
    start_flush: false
redis:
    ip: '127.0.0.1'
    port: 6379
    socket: null
    del_after_read: true
    retention_time: null
    start_flush: true

exchanges:
    BINANCE_FUTURES:
        # trades: [BTC-USDT-PERP]
        ticker: [BTC-USDT-PERP, ETH-USDT-PERP]
        funding: [BTC-USDT-PERP, ETH-USDT-PERP]
        balances: []
        positions: []
        order_info: [] 

# Cryptostore Plugin Interface
plugins:
    grid_protection_strategy:
        module: [cryptostore.strategy.grid_protection_strategy, GridProtectionStrategy]
        config: sandbox/strategy_config.yaml 
